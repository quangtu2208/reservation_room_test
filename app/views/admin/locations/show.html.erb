<%= provide :title, @location.name %>

<%= render "admin/locations/modal" %>
<div class="row grid-margin">
  <div class="col-lg-12">
    <div class="card">
      <div class="card-body">
        <h4 class="card-title">
          <%= t ".information" %>
          <% unless @location.status %>
            <%= button_tag t(".activate_but"), class: "btn btn-success btn-sm float-right",
              data: {toggle: "modal", target: "#activateModal"} %>
          <% end %>
        </h4>
        <%= label_tag nil, @location.location_type_name, class: "badge badge-warning" %>
        <%= @location.name %>
        <address>
          <p>
            <i class="mdi mdi-map-marker">
              <%= @location.national %>, <%= @location.location %>
            </i>
          </p>
        </address>
        <%= t ".description" %>
        <p>
          <%= @location.description %>
        </p>
      </div>
    </div>
  </div>
</div>

<div class="row grid-margin">
  <div class="col-lg-12">
    <div class="card">
      <div class="card-body">
        <h4 class="card-title"><%= t ".gallery" %></h4>
        <div id="lightgallery" class="row lightGallery">
          <% @location.pictures.each do |picture| %>
            <%= link_to picture.url, class: "image-tile" do %>
              <%= image_tag picture.url, class: "image small" %>
            <% end %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>


