<tr>
  <td><%= link_to location_type.name, admin_location_type_path(location_type) %></td>
  <td><%= l location_type.created_at, format: :full %></td>
  <td><%= l location_type.updated_at, format: :full %></td>
</tr>
